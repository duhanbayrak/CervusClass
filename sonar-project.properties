sonar.projectKey=duhanbayrak_CervusClass
sonar.projectName=CervusClass
sonar.sources=.

# Exclude non-production files from ALL analysis (including duplication)
# sonar.cpd.exclusions is NOT supported by SonarCloud automatic analysis,
# so all SQL/migration/script files must go in sonar.exclusions instead.
sonar.exclusions=supabase/**,restore_script.sql,.next/**,node_modules/**,scripts/**
