sonar.projectKey=duhanbayrak_CervusClass
sonar.projectName=CervusClass
sonar.sources=.

# Exclude generated/data SQL files and non-production files from all analysis
sonar.exclusions=\
  supabase/migrations_generated/**,\
  supabase/migration_script_final.sql,\
  restore_script.sql,\
  .next/**,\
  node_modules/**,\
  scripts/**

# Exclude SQL files and scripts from duplication detection
# SQL migration files are data/schema files, not application code
sonar.cpd.exclusions=\
  supabase/**,\
  restore_script.sql,\
  scripts/**

# New code definition: use previous version as reference
sonar.newCode.referenceBranch=main
